[train]
max_tokens_per_batch = 100000

[train.optim]
name = "adam"
lr = 0.0012
betas = [0.9, 0.999]
eps = 1e-8

[train.model]
d_model = 256
num_heads = 4
num_layers = 6
dim_feedforward = 1024
dropout = 0.1

[train.lr_scheduler]
type = 'inverse_sqrt'
warmup_steps = 4000
warmup_decay = 0.01
